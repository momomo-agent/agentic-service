# Fix package.json imports map: add agentic-sense entry

## Progress

Verified: `package.json` imports already has `"agentic-sense": "./src/runtime/adapters/sense.js"`. `src/runtime/adapters/sense.js` exists and exports `createPipeline`. No changes needed.
