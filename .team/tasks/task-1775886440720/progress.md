# 构建 Admin UI dist — 运行 npm run build 生成 dist/admin/

## Progress

Ran `npm run build`. Previous dist was stale (missing ExamplesView). Fresh build succeeded with 23 assets + index.html in dist/admin/.
