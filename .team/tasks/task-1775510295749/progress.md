# 创建 profiles/default.json

## Progress

- File already existed with platform-specific profiles
- Added required default profile with id="default", match={gpu:"none"} per design spec
- Existing catch-all match:{} entry preserved
