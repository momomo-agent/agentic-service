# Design: README completeness

## File
`README.md`

## Required sections
1. **Install** — npx, global npm, Docker commands
2. **API Endpoints** — POST /api/chat, POST /api/transcribe, POST /api/synthesize, GET /api/status, GET/PUT /api/config
3. **Config** — profile selection, model override, fallback config

## No new code — documentation only.
