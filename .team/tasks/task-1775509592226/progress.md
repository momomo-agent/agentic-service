# Progress

`detect(frame)` export already present in `src/runtime/sense.js` (lines 43-51).
- Returns `{ faces, gestures, objects }` normalized from pipeline result
- Guards against null pipeline (returns empty arrays)

No code changes needed. Export verified as present.
