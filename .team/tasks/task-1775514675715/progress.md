# src/runtime/stt+tts.js实现

## Progress

Verified both files match design. No changes needed.

- stt.js: adapter fallback chain correct, EMPTY_AUDIO and 'not initialized' errors correct
- tts.js: adapter fallback chain correct, EMPTY_TEXT and 'not initialized' errors correct
