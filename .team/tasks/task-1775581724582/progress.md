# Add README.md to project root

## Progress

README.md already exists at project root with all required content:
- `npx agentic-service` quickstart ✅
- `docker run -p 3000:3000 momomo/agentic-service` ✅
- All API endpoints documented (POST /api/chat, POST /api/transcribe, POST /api/synthesize, GET /api/status, GET/PUT /api/config) ✅

No changes needed.
