# DBB 规范修复

## Progress

- brain.js: Fixed `content` → `text` in Ollama path (line 43) and OpenAI path (line 90)
- profiles.js: CDN URL updated `momomo-ai` → `momo-ai`
- No `store.del(` direct calls found outside memory.js
