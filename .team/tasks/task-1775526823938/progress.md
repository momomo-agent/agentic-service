# optimizer.js hardware-adaptive config output

## Progress

- Replaced Ollama setup code with hardware-adaptive `optimize(hardware)` function
- Handles apple-silicon, nvidia, cpu-only cases with edge case fallbacks
