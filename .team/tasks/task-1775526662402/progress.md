# cpu-only profile in profiles/default.json

## Progress

- Added cpu-only profile to `profiles/default.json` before catch-all entry
- `matcher.js` already handles `gpu.type === 'none'` — no changes needed
