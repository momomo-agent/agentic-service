# 用户文档 README

## Progress

README.md already existed with install, quick start, API reference, env vars, and config sections.

Added:
- Docker Deployment section with `docker run` command and `docker-compose.yml` example
- Profiles section explaining profiles/default.json fields (match + config)

All acceptance criteria met:
- Three install methods with copyable commands ✓
- Each API endpoint has request/response examples ✓
- Docker section has complete runnable commands ✓
